import './globals.css';

export const metadata = {
  title: 'App Icon Generator',
  description: 'Generate App Icons for AppStore and Google Play',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
